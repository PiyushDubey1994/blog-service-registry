---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: blogsite-api-gateway-service-deployment-loadbalancer
  labels:
    app: blogsite-api-gateway-service-loadbalancer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: blogsite-api-gateway-service-loadbalancer
  template:
    metadata:
      labels:
        app: blogsite-api-gateway-service-loadbalancer
    spec:
      containers:
        - name: blogsite-api-gateway-service-loadbalancer
          image: piyushdubey1994/blogsite-api-gateway-service:tagversion
          ports:
            - containerPort: 8081

---
apiVersion: v1
kind: Service
metadata:
  name: blogsite-api-gateway-service-loadbalancer
spec:
  type: LoadBalancer
  selector:
    app: blogsite-api-gateway-service-loadbalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081